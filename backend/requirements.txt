fastapi
uvicorn[standard]
motor
beanie
pydantic[email]
pydantic-settings
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt
httpx
python-multipart
python-magic
bleach
phonenumbers
cryptography
cloudinary
resend
reportlab
anthropic

# Meta (Facebook / Instagram) Lead Ads integration uses `httpx` (already listed above)
# for Graph API calls. Auto-sync uses Python's built-in asyncio (no node-cron needed).
